![coverage](https://raw.githubusercontent.com/dzherb/mifi-bank-system/badges/.badges/main/coverage.svg)
___
## REST-сервис: банковское приложение на Go, позволяющее управлять пользователями, счетами, картами, кредитами и операциями.

### 🛠 Технологии и стек
- Язык программирования: `go 1.24.1`
- Маршрутизация: `gorilla/mux`.
- Работа с БД: `pgx`.
- Аутентификация: `JWT (golang-jwt/jwt/v5)`.
- Логирование: `logrus`.
- Шифрование: `bcrypt, HMAC-SHA256, PGP`.
- Дополнительно: `gomail.v2 (отправка email), beevik/etree (парсинг XML)`
- База данных: `PostgreSQL`

### 🛢 Схема БД
Доступна [здесь](/database.md)

### 📌 Список эндпоинтов
#### Авторизация
* `/health` `GET` - проверка состояния сервера, JWT-токен не нужен
* `/register` `POST` - регистрация, JWT-токен не нужен
* `/login` `POST` - аутентификация, JWT-токен не нужен
#### Счета
* `/analytics` `GET` - получить аналитику
* `/accounts/all` `GET` - получить список своих счетов
* `/accounts` `POST` - создать счет
* `/accounts/{id}` `GET` - получить счет но id
* `/accounts/{id}/predict` `GET` - прогноз баланса
* `/accounts/debet` - выполнить операцию дебета (📧 будет отправлено письмо)
* `/accounts/credit` - выполнить операцию кредита (📧 будет отправлено письмо)
* `/accounts/transfer` - выполнить перевод (📧 будет отправлено письмо)
#### Карты
* `/cards` `GET` - получить список карт
* `/cards` `POST` - выпустить карту (📧 будет отправлено письмо)
* `/cards/{id}` `GET` - получить карту
#### Кредиты
* `/credits` `GET` - получить список кредитов пользователя
* `/credits` `POST` - выдать кредит
* `/credits/{id}` `GET` - получить информацию о кредите
* `/credits/{id}/schedule` `GET` - график платежей по кредиту